// Generated by CoffeeScript 1.12.7
(function() {
  module.exports = _exv('tabForm', 'form', {
    num: 0,
    saveByStep: true,
    renderOneTab: function() {
      var curItems, ref;
      curItems = this.items[this.num];
      if (_.isFunction(curItems.items)) {
        curItems.items = curItems.items.call(this);
      }
      this.genForm(curItems.items);
      this.btns = curItems.btns;
      this.$('.btnCtn').empty();
      this.addBtns();
      if (curItems.info) {
        this.msg(curItems.info, 'info');
      }
      if ((ref = curItems.callback) != null) {
        ref.call(this);
      }
      this.formTab.find("li:eq(" + this.num + ")").addClass('active').siblings().removeClass('active');
      if (!this.formDlgFirstTab) {
        return this.afterRender();
      }
    },
    single: function() {
      return _.isString(this.items[0]);
    },
    curItems: function() {
      if (this.single()) {
        return this.items;
      } else {
        return this.items[this.num].items;
      }
    },
    addItems: function(it, index) {
      if (index) {
        return this.curItems().splice(index, 0, it);
      } else {
        return this.curItems().push(it);
      }
    },
    removeItems: function(it) {
      return this.curItems().remove(it);
    }
  });

}).call(this);

//# sourceMappingURL=genTabForm.js.map
