// Generated by CoffeeScript 1.12.7
(function() {
  var portal;

  module.exports = portal = cf.view.portal = cf.view.tag.extend({
    parent: '#content',
    plugins: [],
    preRender: function() {
      var act, i, it, len, ref, results;
      ref = _.result(this, 'plugins');
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        it = ref[i];
        if (it.type) {
          results.push(new cf.view[it.type](it.attrs));
        } else if (it.tmpl) {
          act = it.cleanAll ? 'html' : 'append';
          results.push($(it.parent)[act](cf.rtp(it.tmpl, it.opt)));
        } else {
          results.push(void 0);
        }
      }
      return results;
    }
  });

  $.extend(cf.dm, {
    portal: function(p, opt) {
      var init;
      init = {
        parent: p,
        auto: true
      };
      return new portal(cf._packOpt(init, null, null, opt));
    }
  });

}).call(this);

//# sourceMappingURL=portal.js.map
