// Generated by CoffeeScript 1.12.7
(function() {
  cf.view.itemSelect = cf.view.collection.extend({
    toFetch: false,
    entity: 'cat',
    color: 'primary',
    stSync: true,
    type: 'checkbox',
    events: {
      'click input': function(e) {
        var d, t;
        d = this.findData(e).toJSON();
        if (this.multi) {
          t = util.ct(e);
          t.toggleClass('active');
        } else {
          util.setActive(e);
        }
        return typeof this.afterPick === "function" ? this.afterPick(d, e) : void 0;
      }
    },
    criteriaOpt: function() {
      return {
        q: {
          type: this.type
        }
      };
    },
    modelOpt: function() {
      var type;
      type = this.type;
      return {
        className: "btn btn-" + this.color + "-outline m-r-1 m-t-1",
        tagName: 'input',
        setContent: function() {
          this.$el.attr('code', this.data.code);
          this.$el.attr('type', type);
          return this.$el.append(this.data.title);
        }
      };
    }
  });

}).call(this);

//# sourceMappingURL=itemSelect.js.map
