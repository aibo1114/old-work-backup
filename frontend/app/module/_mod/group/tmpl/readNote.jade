!= f.tmpl('userAvatar', {user:user,subTitle: weekDate})

hr

each t in tasks
    h3.text-xs-center= t.title
    each it,i in t.items
        if t.perWeek > 1
            h4.text-xs-center Day #{i+1}
        != f.tmpl('threadItem', it)

.text-xs-center.m-y-3.dBtns