// Generated by CoffeeScript 1.12.7
(function() {
  var PROTO_PATH, action, center, grpc, main, notify;

  PROTO_PATH = '/Users/alex/Pictures/grpc/center/center.proto';

  grpc = require('grpc');

  center = grpc.load(PROTO_PATH).center;


  /**
   * Implements the SayHello RPC method.
   */

  action = function(call, cc, callback) {
    callback(null, {
      message: 'Hello ' + call.request.name
    });
  };

  notify = function(call, bb, callback) {
    callback(null, {
      message: 'Hello ' + call.request.name
    });
  };


  /**
   * Starts an RPC server that receives requests for the Greeter service at the
   * sample server port
   */

  main = function() {
    var server;
    server = new grpc.Server;
    server.addProtoService(center.Center.service, {
      action: action,
      notify: notify
    });
    server.bind('0.0.0.0:50051', grpc.ServerCredentials.createInsecure());
    server.start();
  };

  main();

}).call(this);

//# sourceMappingURL=greeter_server.js.map
