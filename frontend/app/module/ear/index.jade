extends ./inc/main

block style
    :less(minify=true)
        /*.media {*/
        /*padding-top: 10px;*/
        /*}*/
block content
    if head
        +slide(c, head.headItem, null, true)#carousel-example-generic.mb.slide.banner
    .m3.row.xrow
        each v,k in _cd
            if k != 'post'
                .col-md-4
                    +panel(i18.ii(k),'panel-success','/entityList?entity=' + k)
                        .list-group
                            +media(locals[k + 'List'],{func: v.func, text: v.text})